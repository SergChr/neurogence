import {
	StyleSheet,
} from 'react-native';

import { Colors } from './enums';

export default StyleSheet.create({
  screen: {
		flex: 1,
    padding: 0,
    backgroundColor: Colors.Background,
	},
});
