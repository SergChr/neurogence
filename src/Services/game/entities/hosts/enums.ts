export enum HostTypes {
  MailServer = 'mailServer',
  Camera = 'camera',
  Router = 'router',
  PC = 'PC',
  Database = 'database',
  Localhost = 'localhost',
}

export enum Upgrades {
  ExploitVersion = 'exploitVersion',
}
